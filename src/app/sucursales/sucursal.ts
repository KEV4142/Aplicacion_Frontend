export interface SucursalDTO {
    sucursalID: number;
    descripcion: string;
    direccion: string;
    coordenada: string;
    estado: string;
}
export interface SucursalCreacionDTO {
    descripcion: string;
    direccion: string;
    coordenada: string;
    estado: string;
  }